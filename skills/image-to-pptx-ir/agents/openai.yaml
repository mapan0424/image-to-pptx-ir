interface:
  display_name: "Image to PPTX-IR"
  short_description: "Turn slide images into validated editable PPTX plans"
  default_prompt: "Use $image-to-pptx-ir to convert this slide image into validated semantic and render JSON for an editable PowerPoint rebuild."

policy:
  allow_implicit_invocation: true

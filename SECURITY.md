# Security policy

Report suspected vulnerabilities privately through GitHub's security advisory feature. Do not include private slide content, credentials, embedded image data, or confidential presentation files in public issues.

The preview command writes local SVG. Treat untrusted image `href` values and inline SVG as untrusted content; review generated files before opening them in privileged environments.

# Examples

| Example | Purpose | Elements | Highlights |
| --- | --- | ---: | --- |
| `ai-operations-system` | Featured fidelity fixture | 210 | Five layers, side panels, department matrix, 45 Lucide icons, multiline text, dashed loop |
| `cluster-communication` | Minimal quick start | 6 | Groups, connectors, icon binding, safe text |

Each example includes semantic and render JSON. Generated SVG previews are deterministic diagnostic artifacts; the featured example also includes a PNG for GitHub rendering.

The featured example is deliberately organization-neutral. Tests prevent removed brand placeholders from reappearing in its public JSON.
